<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ url("homepage") }}">
                Щедро &raquo; Склад
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {% if is_granted('ROLE_USER') %}
                    {{ knp_menu_render('AppBundle:Builder:mainMenu', {'currentClass': 'active'}) }}
                {% endif %}
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ url("fos_user_security_logout") }}"><span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> {{ 'link_logout'|trans({}, 'SonataUserBundle') }}</a></li>
                </ul>
            {% elseif is_granted('IS_AUTHENTICATED_ANONYMOUSLY') %}
                <ul class="nav navbar-nav navbar-right">
                    <li role="separator" class="divider-vertical"></li>
                    <li><a href="{{ url("fos_user_security_login") }}"><span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> {{ 'link_login'|trans({}, 'SonataUserBundle') }}</a></li>
                    <li role="separator" class="divider-vertical"></li>
                    <li><a href="{{ url("fos_user_registration_register") }}"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> {{ 'link_register'|trans({}, 'SonataUserBundle') }}</a></li>
                </ul>
            {% endif %}
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>